package com.vectorialdata.app.feature.portfolio

import androidx.compose.runtime.Composable
import androidx.compose.ui.Modifier
import com.vectorialdata.app.feature.common.PlaceholderScreen

@Composable
fun PortfolioScreen(modifier: Modifier = Modifier) {
    // M3: positions list + allocation + dividends.
    PlaceholderScreen("Portfolio", "Posiciones y asignación llegan en el próximo milestone.", modifier)
}
